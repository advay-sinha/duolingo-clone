# Backend dependencies.

fastapi==0.121.2          # web framework: routing, dependency injection, OpenAPI
uvicorn[standard]==0.42.0 # ASGI server used to run the app in development
pydantic-settings==2.13.1 # typed, env-var-driven configuration (Settings class)
sqlalchemy==2.0.52        # ORM and Core; SQLite is reached via Python's stdlib sqlite3
bcrypt==5.0.0             # password hashing. Adaptive and self-salting -- a password
                          # hash must be *slow*, which is exactly what a general-purpose
                          # digest is not. Sessions need no library: `secrets` for the
                          # token, `hashlib` for its digest.

# --- development / test only ---
pytest==8.4.2             # test runner
httpx==0.28.1             # required by fastapi.testclient to drive the app in-process
